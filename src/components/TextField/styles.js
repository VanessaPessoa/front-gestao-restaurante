import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    box:{
        marginBottom: 15
    },
     input:{
        width: "100%",
    },
    messageError: {
        fontSize:"12px",
        color: "#D95245"
    }
});